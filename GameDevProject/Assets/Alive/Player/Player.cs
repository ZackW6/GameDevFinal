using System;
using UnityEngine; 
[RequireComponent(typeof(CameraManager))]
public class Player : Character
{
    void Start()
    {
        
    
     
     

    }

    // Update is called once per frame
    void Update()
    {
       
    }
}
